# ⚠️ Deprecated — Use .agents/rules

이 폴더(`.agent/rules`)는 구버전 Antigravity 호환용으로만 유지됩니다.

**현재 활성 규칙 폴더: `.agents/rules/`**

Antigravity는 `.agents/rules`(복수형)를 기본값으로 사용합니다.
이 폴더는 하위 호환성을 위해 유지되지만, 규칙 수정은 `.agents/rules/`에서만 하세요.
